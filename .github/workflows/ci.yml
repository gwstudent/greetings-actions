name: CI
on:
  push:
    branches: [ test ]
jobs:
  container-test-job:
    runs-on: ubuntu-latest
    steps:
     - name: Log the step container node version
       uses: docker://node:20-alpine3.16
       with:
         entrypoint: usr/local/bin/echo
         args: -v
