
name: containers-demo

on:
  push:
    branches: [ "xmllint" ]
  pull_request:
    branches: [ "xmllint" ]

jobs:
  lint-tool:

    runs-on: ubuntu-latest
   
    steps:
     - name: run xmllint
       uses: docker://quay.io/techupskills/roardebug:1.0.2 
       with:
         entrypoint: xmllint
             

