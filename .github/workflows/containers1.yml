
name: containers-demo

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  info:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build with Gradle
      run: "uname -a"

