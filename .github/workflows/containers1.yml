
name: containers-demo

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  info:

    runs-on: ubuntu-latest
   # container: rockylinux:9.1
    steps:
    - uses: actions/checkout@v3
    - name: get info
      run: "go version"

